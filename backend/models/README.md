Run `notebooks/house_price_model.ipynb` end-to-end (Kernel -> Restart & Run All).
Its last cell saves `house_price.pkl` and `locations.json` into the `notebooks/`
folder — copy both files into this `backend/models/` directory before starting
the API. They are intentionally not committed here / to git (see root .gitignore)
since they are build artifacts generated from the dataset.
